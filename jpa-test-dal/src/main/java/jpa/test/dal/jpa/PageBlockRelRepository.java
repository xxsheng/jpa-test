package jpa.test.dal.jpa;

import jpa.test.model.pojo.PageBlockRel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PageBlockRelRepository extends JpaRepository<PageBlockRel, Long> {
}
